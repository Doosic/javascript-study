# 연산자 마무리

- ?? - null 병합 연산자
  || 와 달리, falsy가 아닌 null 또는 undefind만 대체
  조금 엄격하다!

  ex) let x = undefind;
  x ?? "x에 값이 없습니다." = x에 값이 없습니다.

  ex) x = 0;
  x ?? "x에 값이 없습니다." = 미출력

  ## 연산자의 우선순위

- 필요할 때 마다 찾아보면서 사용하자
- 1.  !, +, -, ++, --, typeof
- 2.  \*\*
- 3.  \*, /, %
- 4.  +, -
- 5.  <, <=, >, >=
- 6.  ==, !=, ===, !==
- 7.  &&
- 8.  ||
- 9.  =, +=, -=, \*=, /=, %=, \*\*=, &&=, ||=, ??=
- 10. ,
